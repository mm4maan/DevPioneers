@model IEnumerable<DevPioneers.Models.QUESTION>

@{
    ViewBag.Title = "Index1";
    Layout = "~/Views/Shared/_MetroLayout.cshtml";
}

<h2>Index1</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table>
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.QuestionTitle)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.QuestionBody)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.AskedDate)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.AskedBy)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.QuestionTitle)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.QuestionBody)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.AskedDate)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.AskedBy)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id })
        </td>
    </tr>
}

</table>
